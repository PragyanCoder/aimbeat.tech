# Flask and essential dependencies
Flask==3.1.0
flask-login
flask_sqlalchemy
gunicorn
werkzeug==3.1.3
python-dotenv==1.0.1
requests==2.32.3
jinja2==3.1.5

# Database (PostgreSQL or SQLite)
psycopg2-binary  # PostgreSQL support (only if using PostgreSQL)

# Security
bcrypt  # For password hashing (optional)
cryptography==38.0.4  # If needed for secure operations

# Frontend Assets and Animations
flask-assets  # Manages CSS and JS assets
libsass  # Compiles SCSS to CSS if using Sass
pillow==11.1.0  # Image processing (logos, thumbnails)

# Additional Utilities
colorama==0.4.6  # Colored logging (useful for debugging)
